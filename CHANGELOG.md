# Changelog

## v1.1.1 - 2026-04-27

- 修复 `/关闭模式` 后首条普通消息仍继承旧上下文的问题；关闭模式时现在会同时清空当前对话历史。
- 修复 `default_mode` 开启时 `/关闭模式` 可能被默认模式重新激活的问题；当前会话会显式保持 `off`。
- 移除 `/关闭模式` 对 `reset` 的别名，避免与 AstrBot 内置 `/reset` 命令冲突。
- 更新 `README.md`，补充关闭模式行为说明以及不要与额外角色扮演类 `system prompt` 混用的注意事项。

## v1.1.0 - 2026-04-27

- 将插件行为从“仅首轮拼接”改为“会话内持续注入”，切换一次模式后后续普通消息也会自动带上对应提示词。
- 新增 `on_llm_request` 钩子，在发送给 LLM 的请求阶段统一追加角色扮演或思维模式提示词。
- 调整 `/角色扮演` 与 `/思维模式` 指令逻辑：既可切换模式，也可在切换时直接发送当前消息。
- 新增 `/关闭模式` 指令，用于停止当前会话的自动提示词注入。
- 新增 `default_mode` 配置项，可为新会话设置默认模式。
- 当前版本改为优先向 `user prompt` 尾部追加 Marker，以贴近 DeepSeek 文档的推荐用法。
- 插件会持久保存切换命令后附带的首轮消息；若 AstrBot 没有把完整历史带回，插件会自动补回首轮消息来模拟历史记忆。

## v1.0.0 - 2026-04-27

- 新增 `/角色扮演` 指令，接收首轮对话并在末尾追加空行与 `INNER_OS_MARKER` 后发送给 LLM。
- 新增 `/思维模式` 指令，接收首轮对话并在末尾追加空行与 `NO_INNER_OS_MARKER` 后发送给 LLM。
- 新增 `_conf_schema.json`，支持在 AstrBot 面板中可视化配置 `roleplay_prompt` 与 `thinking_prompt`。
- 更新 `metadata.yaml`，将插件元信息从模板内容改为当前项目配置。
- 更新 `README.md`，补充指令用法、配置项说明和首轮拼接行为说明。
